name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # 1. Typst ビルド
      # cetz 0.3.x (fletcher が依存) が Typst 0.14+ の path 関数変更で壊れるため 0.13 系に固定
      - uses: typst-community/setup-typst@v5
        with:
          typst-version: "0.13.1"

      - name: Compile Typst slides
        run: |
          mkdir -p public/slides
          for f in typst/*.typ; do
            [ -f "$f" ] || continue
            typst compile --font-path typst/fonts "$f" "public/slides/$(basename "${f%.typ}").pdf"
          done

      # 2. Node.js セットアップ
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      # 3. Astro ビルド（Zenn/Qiita API取得もこの中で実行）
      - run: npm ci

      - name: Generate OG images
        run: npm run og:build

      - name: Build Astro
        run: npm run build
        env:
          ZENN_USERNAME: ${{ vars.ZENN_USERNAME || 'ahoxa1rx' }}
          QIITA_USERNAME: ${{ vars.QIITA_USERNAME || 'raiga0310' }}

      # 4. デプロイ（Cloudflare Workers）
      - name: Deploy to Cloudflare Workers
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
